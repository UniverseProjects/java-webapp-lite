apply from: gradle.rootProject.baseScriptPath

apply plugin: 'war'

dependencies {
    compile project(':UPCommon')
    compile project(':UPLog')

    compile 'org.reflections:reflections:0.9.10'
    providedCompile 'javax.servlet:jsp-api:2.0'
}